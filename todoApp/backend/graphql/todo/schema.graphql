type Todo {
  _id: ID!
  title: String!
  state: Boolean
}

type Query {
  todos: [Todo]
  todo(id: ID!): Todolist 
}

type Mutation {
    createTodo(title: String!): Todolist
    removeTodo(id: ID!): Todolist
}